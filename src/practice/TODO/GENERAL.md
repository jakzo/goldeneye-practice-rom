- State loading
    - Finish all the missing bits and bugs
- Lag monitor
    - Small graph of speed loss
- Deterministic replay system
    - Global setting that defaults to disabled which when enabled will record all inputs and frame data for the next played level
        - The start of a recording is a header that stores:
            - Magic string `GERP`
            - Recording file format version
            - Total recording byte size
            - Game version (US, EU, JP)
            - Level ID
            - Initial gameplay seeds as set at start of level
            - Flags (eg. does recording include seed values in each frame?)
        - When recording, each frame it saves:
            - speedgraphframes (u8)
            - controller 1 inputs (u16 + u8 * 2? I think the stick range is -80 to 80?)
            - the 2 gameplay seeds (not the TLB one) (optional for validation, u64 * 2)
                - Q: Are the seeds thread safe? Meaning processing the next gameplay frame with the same speedgraphframes and seeds will always result in the same gameplay and final seed value? They are not used by the audio thread or anything which will throw the seed off mid-tick?
        - Stream to SRAM
            - Writes to same position as save states (will overwrite existing save state or recording)
            - Dual 256 byte buffers and queue up to async copy the buffer to SRAM via DMA while writing to other buffer? I want to minimise gamplay lag while this writes to SRAM so let me know how viable this is or if it is unnecessary.
        - Pay attention to sizing of the fields in the file format, it must be as small as possible to fit into SRAM
            - Once seeds are validated and removed, should be 4 bytes per frame which equals 11.7kb/min or max of ~10 minutes
            - Max of ~5 minutes when seeds are stored
        - Log when recording starts and ends
        - Reset recording setting back to off after recording starts
    - Global setting that defaults to disabled which when enabled will playback the recorded run for the level
        - Can not be enabled if no recorded run or if it is not for this level
        - On level start forces seeds to saved values
        - Each updateFrames call forces speedgraphframes/deltaFrames and controller inputs to be the saved values for the frame
        - If seeds are present in recording, verify current seed values against the saved seed values for the frame and emit error log on first frame where they diverge (continue playback though)
        - Cancel playback of inputs and start using real controller inputs if a button other than the hotkey trigger is pressed on the real controller
        - Log when replay starts and ends
        - Reset recording playback setting back to off once the playback starts
    - Write a test case that records some simple gameplay (bunker, walk forwards slightly, press B (to open door), shoot a few times (kills a guard behind the door), waiting 20 frames between each of those and 60 at the end) then restarts the level and plays it back
- Align lag
    - Measure the amount of lag the original ROM gets compared to the practice ROM
    - Implement efficiency improvements to bring the practice ROM's lag roughly in line with the original
- Dam gate guard indicator
    - Show their speed/progress to the gate
- TLB improvements
- Splits
    - Hotkey to enter splits editor
    - Shows split regions in the world
    - Split regions can be added, edited and removed
    - Splits can be saved/loaded from SD card
    - Store and show split PBs and comparisons
- Grenade cam
    - Bug: explosion and smoke textures get messed up
- Figure out frame delta determinism after load
